name = "wappu-lingo"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# D1 Database binding
[[d1_databases]]
binding = "DB"
database_name = "wappu-lingo-db"
database_id = "a8c64848-f8fc-4662-980f-5304dd2ff6bc"

# Note: Secrets Store bindings for Pages are configured via the Cloudflare Dashboard
# or using wrangler pages secret commands. The secrets_store_secrets config
# is for Workers, not Pages Functions.
# Configure GRAVATAR_CLIENT_ID as an environment variable in Cloudflare Pages settings.
